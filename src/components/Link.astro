---
const { href, text, class: className = "link", icon, alt = "" } = Astro.props;
---

<a href={href} class={className}>
  {text}
  {icon && (
    <img 
      src={icon} 
      alt={alt} 
      aria-hidden={alt ? "false" : "true"} 
      class="inline-block" 
    />
  )}
</a>
