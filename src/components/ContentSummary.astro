---
const { roles, industry, year } = Astro.props
---

<section>
	<div class='container'>
		<div class='content__info'>
			<span>
				<p class='p2'>Role</p>
				{roles ? roles.map((role, i) => <p>{role}</p>) : null}
			</span>
			<span>
				<p class='p2'>Industry</p>
				<p>{industry}</p>
			</span>
			<span>
				<p class='p2'>Timeline</p>
				<p>{year}</p>
			</span>
		</div>
	</div>
</section>
