---
const { title, links } = Astro.props
---

<section>
	<div class='container'>
		<div class='link__group'>
			<h2>{title}</h2>
			{
				links &&
					links.map(({ href, label }) => (
						<a
							class='link'
							target='_blank'
							href={href}
						>
							{label}
						</a>
						<hr class="hr-subtle" />
					))
			}	
		</div>
		
	</div>
</section>
